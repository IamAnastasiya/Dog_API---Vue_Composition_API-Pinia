<script setup lang="ts">
    import BreedData from '../../models/BreedData';

    const props = defineProps({
        info: Object as () => BreedData
    })

    
</script>


<template>
    <div class="wrapper">
        <span class="title">{{ props.info!.name }}</span>
        <p class="mission">{{ props.info!.description }}</p>
        <div class="details">
            <div class="details-column">
                <p class="category-title">Temperament:</p>
                <p>{{ props.info!.temperament }}</p>
            </div>
            <div class="details-column">
                <div v-if="props.info!.origin"><span class="category-title">Origin: </span>{{ props.info!.origin }}</div>
                <div><span class="category-title">Weight: </span>{{ props.info!.weight.metric }} kg</div>
                <div><span class="category-title">Life span: </span>{{ props.info!.life_span }} years</div>
            </div>
        </div>

    </div>
</template>


<style lang="scss" scoped>
    .wrapper {
        border-radius: 20px;
        border: 2px solid $pale-coral;
        padding: 26px 40px 40px;
        position: relative;
        margin-bottom: 55px;
    }


    .title {
        @include size-color-weight-height(36px, #1D1D1D, 500, 52px);
        padding: 5px 20px;
        background-color: #FFF;
        position: absolute;
        top: -35px;
        left: 50%;
        transform: translateX(-50%);
        white-space: nowrap;
    }


    .mission {
        @include size-color-weight-height(20px, $dark-grey, 400, 28px);
        text-align: center;
        margin: 0 0 20px;
    }


    .details {
        display: flex;
        column-gap: 20px;
    }

    .details-column {
        max-width: 270px;
        width: 100%;
        font-weight: 400;
        color: $dark-grey;
    }

    .category-title {
        @include size-color-weight-height(16px, #1D1D1D, 600, 23px);
        margin: 0;
    }

    .details-column:nth-of-type(2) {
        display: flex;
        flex-direction: column;
        gap: 10px;
    }


    @media (max-width: 500px) {
        .wrapper {
            padding: 19px 20px 20px;
            margin: 0;
        }

        .title {
            @include size-color-weight-height(20px, #1D1D1D, 700, 48px);
        }

        .details {
            flex-direction: column;
        }

        .details-column:nth-of-type(1) {
            margin-bottom: 10px;
        }
    }

    @media (max-width: 368px) {

        .wrapper {
            padding: 25px 15px 15px;
        }
    }

</style>